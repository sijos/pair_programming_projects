<% current_user.id %>

<button data-user-id="<%= user.id %>"
  data-initial-follow-state="<%= current_user.follows?(user) ? 'followed': 'unfollowed' %>"
  class="follow-toggle" type="button" name="button"></button>
